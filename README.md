# Check IPs for blacklist
This script checks a group of ip addresses and sends email if has any blacklisted ip.

### Usage
```bash
./main.py
```

### Configuration
To configure create config.ini file.
